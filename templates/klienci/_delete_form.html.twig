<form method="post" action="{{ path('app_klienci_delete', {'id': klienci.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ klienci.id) }}">
    <button class="btn">Delete</button>
</form>
